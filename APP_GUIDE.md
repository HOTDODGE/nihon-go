# NihonGo! 로컬 스마트폰 앱 & PC 앱 이용 가이드

`NihonGo!`를 로컬 환경에서 **스마트폰 앱**과 **PC 전용 데스크톱 앱**으로 사용할 수 있도록 세팅되었습니다. 아래 가이드를 참고하여 원하는 환경에서 실행 및 설치하세요.

---

## 1. 🖥️ PC 앱 (Windows 전용 데스크톱 앱)

### 방법 A. 원클릭 실행 (가장 추천)
프로젝트 루트 폴더에 있는 **`run-pc.bat`** 파일을 더블 클릭하면 자동으로 Electron 기반 독립 창 앱이 실행됩니다.

### 방법 B. 터미널 명령어로 실행
프로젝트 루트 폴더에서 아래 명령어 중 하나를 실행합니다:

```bash
# 1. 패키지 설치 (최초 1회)
npm install

# 2-1. 개발 모드 실시간 실행 (코드 수정 시 즉각 반영)
npm run electron:dev

# 2-2. 완성본 앱 미리보기 실행
npm run electron:preview

# 2-3. 윈도우 설치형(.exe) 및 포터블 실행 파일 생성 (release/ 폴더에 생성)
npm run electron:build
```

---

## 2. 📱 스마트폰 앱 (모바일 앱)

스마트폰에서 사용하는 방법은 **[초간편 PWA 설치]**와 **[Android APK 빌드]** 두 가지 방식이 모두 지원됩니다.

### 방식 1. 초간편 무설치 앱 설치 (PWA - 갤럭시 / 아이폰 모두 지원, 강력 추천)
별도의 복잡한 Android Studio나 SDK 설치 없이, 로컬 PC와 같은 Wi-Fi에 연결된 스마트폰에서 즉시 전체화면 독립 앱으로 설치하는 방법입니다.

1. **로컬 서버 구동**:
   PC 터미널에서 로컬 네트워크 공유 모드로 개발 서버를 켭니다:
   ```bash
   npm run dev -- --host
   ```
2. **IP 확인 및 스마트폰 브라우저 접속**:
   - 터미널에 표시되는 `Network: http://192.168.x.x:5173` 주소를 확인합니다.
   - 스마트폰(동일 Wi-Fi 연결 상태)의 브라우저로 해당 주소에 접속합니다.
3. **스마트폰 앱으로 설치**:
   - **삼성 인터넷 / 크롬 (Android)**: 브라우저 주소창 우측의 **'앱 설치'** 아이콘 또는 더보기(⋮) 메뉴에서 **'홈 화면에 앱 추가' / '설치'** 터치
   - **사파리 (iOS / iPhone)**: 하단 공유 버튼(네모에 위 화살표) 클릭 후 **'홈 화면에 추가'** 터치
4. **결과**:
   스마트폰 홈 화면에 `NihonGo!` 전용 앱 아이콘이 생성되며, 터치 시 상단 주소창이 없는 완전한 **전체화면 네이티브 앱**처럼 작동합니다.

---

### 방식 2. 안드로이드 APK 파일 직접 빌드 (Capacitor)
순수 네이티브 Android 패키징 프로젝트를 생성하여 `.apk` 파일을 생성하고자 할 때 사용합니다:

1. **최초 안드로이드 프로젝트 생성**:
   ```bash
   npm run cap:add:android
   ```
2. **웹 빌드 파일 동기화 & Android Studio 열기**:
   ```bash
   npm run cap:android
   ```
3. Android Studio가 열리면 메뉴의 **Build > Build Bundle(s) / APK(s) > Build APK(s)** 를 클릭하여 스마트폰에 직접 설치 가능한 `app-debug.apk` 파일을 생성할 수 있습니다.

---

---

## 3. 🎙️ 오픈소스 일본어 TTS 모델 활용 안내

앱 우측 상단의 **음성 설정(스피커 아이콘)** 모달에서 3가지 엔진을 선택할 수 있습니다:

1. **🌐 시스템 내장 (Web Speech API)**:
   - 기기(OS/브라우저)에 내장된 Google, Microsoft, Apple 일본어 음성을 사용합니다.
2. **🤖 오픈소스 엔진 (100% 무설치 독립형, 추천)**:
   - Windows나 스마트폰에 일본어 언어팩이 전혀 없어도, Web Audio 기반 자체 일본어 50음도 포먼트 합성 엔진으로 **100% 로컬 오프라인에서 정확한 일본어 발음**을 재생합니다.
   - 여성 표준음, 남성음, 즈다몬 스타일 하이톤 선택 가능.
3. **🍃 VOICEVOX 로컬 엔진 (압도적 1위 음질)**:
   - 일본의 대표 무료 오픈소스 음성 합성 소프트웨어인 [VOICEVOX](https://voicevox.hiroshiba.jp/)가 PC에 켜져 있을 때(`http://127.0.0.1:50021`) 자동으로 감지되어, **즈다몬, 시코쿠 메탄, 카스카베 츠무기** 등 최고 품질의 애니메이션 캐릭터 음성으로 회화와 독해를 재생합니다.

---

## 4. 주요 구성 파일 안내

- `electron/main.cjs`: PC 앱 메인 프로세스 (윈도우 크기 및 단축키 F12/F5 지원)
- `src/utils/voicevox.ts`: 오픈소스 VOICEVOX 로컬 API 클라이언트
- `src/utils/openTtsEngine.ts`: 100% 기기 독립형 오픈소스 일본어 Web Audio 음성 합성기
- `src/components/VoiceSettingsModal.tsx`: 다중 TTS 엔진 및 캐릭터 보이스 선택 모달
- `public/manifest.webmanifest`: 스마트폰 및 PC 브라우저 설치형 PWA 앱 설정
- `run-pc.bat`: 윈도우 PC 전용 원클릭 앱 실행 파일
